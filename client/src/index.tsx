import * as React from 'react';
import * as ReactDOM from 'react-dom';
import SlideShow from './components/slideShow';
import './index.css';

ReactDOM.render(
    <SlideShow />, document.getElementById('root')
);
